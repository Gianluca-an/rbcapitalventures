# Netlify build configuration — auto-detected on import.
# (Cloudflare Pages: use the same values in its dashboard —
#  build command "npm run build", output directory "dist".)

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
