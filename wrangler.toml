# Cloudflare deployment config (Workers static assets).
# Serves the built site from dist/ with single-page-app routing so deep links
# like /about and /sectors resolve to index.html.
name = "rbcapitalventures"
compatibility_date = "2025-01-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
